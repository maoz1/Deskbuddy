#pragma once
// =========================================================
// Template for src/secrets.h
// Copy this file to "secrets.h" (same folder) and fill in your
// WiFi credentials. secrets.h is gitignored and never committed.
//
// Use your MAIN home network - the same one the Philips Hue
// bridge is on. Guest networks isolate devices and block the
// bridge, so Hue pairing will fail on them.
// =========================================================
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
